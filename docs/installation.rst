.. highlight:: shell

============
Installation
============


Stable release
--------------

To install predex, run this command in your terminal:

.. code-block:: console

    $ pip install predex

This is the preferred method to install predex, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for predex can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/tomkuipers1402/predex

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/tomkuipers1402/predex/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/tomkuipers1402/predex
.. _tarball: https://github.com/tomkuipers1402/predex/tarball/master
